{
  "name": "busao",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npx lakebed@0.0.29 dev --port ${WEB_PORT:-3000}",
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.testavel.json"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "preact": "^10.29.8",
    "typescript": "^5.8.0"
  }
}
